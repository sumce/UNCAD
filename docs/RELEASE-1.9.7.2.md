# UNCAD v1.9.7.2 U1DWG 机台信息数据库修复

## 修复内容

- Git历史确认回归首次出现在 v1.9.7 新增机台信息和首框视图功能之后。
- 修复新增 `DBText`没有先绑定输出数据库就使用数据库对象ID的问题。
- 写入机台标题和元数据前调用输出数据库的默认值初始化，避免 AutoCAD 返回 `eWrongDatabase`。
- 首框视图失败时单独报告 `首框视图设置失败`，不再与文字写入混淆。
- 本次修复版本为 `1.9.7.2`。
