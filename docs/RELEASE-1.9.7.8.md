# UNCAD v1.9.7.8 U1U 重复设备身份修复

## 修复内容

- 批量 `U1U`不再因多个物理图框使用相同的“机台ID + 设备名”而提前失败。
- 同机台同名图框允许分别更新，正式 BOQ按设备名将其工程量汇总到同一设备列。
- 机台ID或设备名为空仍然会被拒绝。
- `U1DWG`仍按独立图框保留同名设备，不受本次 BOQ汇总规则影响。
- 本次版本为 `1.9.7.8`。
