# UNCAD v1.9.7.7 U1U 项目特征匹配

## 修复内容

- 现有 CAD 清单回退电缆时，不再把清单中显示的型号文本当作固定清单别名比较。
- 改为使用现有清单的“项目特征”列，与固定清单的“项目特征”列进行规范化匹配。
- 匹配成功后使用固定清单行的标准别名作为 U1U 规划型号，并保留其项目编码、项目名称、项目特征和单位。
- 兼容 `\P`换行、空白、`mm²/mm2`和常见 `o.6/0.6`显示差异。
- 两边都无法匹配时继续抛出明确异常。
- 本次版本为 `1.9.7.7`。
