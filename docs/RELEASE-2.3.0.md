# Release 2.3.0

## 修复

- 延迟解析 QuickLine 的 AutoCAD `RXClass`，让纯文本处理和核心测试不依赖
  AutoCAD 运行时提前加载。
- 修复批量 `U1U` 更新时重复打开清单对比窗口的问题。
- 修复 `U1U` 对比窗口的 `Dock` 布局顺序，左侧图框列表不再覆盖清单表格。
- 为以上行为增加回归测试。

## 发布校验

- 统一版本：`2.3.0` / `2.3.0.0`。
- 仅生成 `UNCAD Pro` 在线授权包。
- 测试与打包命令见 `docs/RELEASE.md`。
