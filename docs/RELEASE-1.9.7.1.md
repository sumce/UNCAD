# UNCAD v1.9.7.1 U1DWG 数据库错误修复

## 修复内容

- 修复 `U1DWG` 执行时出现 `eWrongDatabase` 的问题。
- 同一机台所有图框一次性从源图执行 `Wblock`，不再跨临时数据库传递 `ObjectId`。
- 在输出数据库内部完成图框横向排列、首框视图设置和机台信息写入。
- 增加源实体数据库归属校验和分阶段错误信息，便于定位 AutoCAD 数据库异常。
- 本次修复版本递增为 `1.9.7.1`。
