# UNCAD v1.9.7.5 U1U 电缆型号回退

## 修复内容

- `U1U`先尝试匹配机台 Excel/块属性中的原始电缆型号。
- 原始型号无法匹配时，从当前图框已有清单表读取用户实际选择的电缆型号。
- 例如原始型号为 `3*25用4*14代`、清单表型号为 `3*25`时，使用清单表型号继续生成。
- 两个数据源都无法匹配固定清单时，在 CAD 事务写入前抛出明确异常，不生成不完整 BOQ。
- 本次版本为 `1.9.7.5`。
